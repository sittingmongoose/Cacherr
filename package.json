{
  "name": "cacherr-websocket-tests",
  "version": "1.0.0",
  "description": "WebSocket testing for Cacherr",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}