{
  "_comment": "Copy to .cursor/mcp-config.json and inject secrets locally (recommended via 1Password).",
  "mcpServers": {
    "context7": {
      "httpUrl": "https://mcp.context7.com/mcp",
      "headers": {
        "CONTEXT7_API_KEY": "REPLACE_ME"
      }
    }
  }
}
