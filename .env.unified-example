# Example environment configuration for unified mounts (hardlink support)
# This configuration enables hardlinks to work properly by using a single
# filesystem mount point that contains all necessary directories.

# Plex Configuration
PLEX_URL=http://your-plex-server:32400
PLEX_TOKEN=your_plex_token_here

# Unified Mount Configuration
# This should be a parent directory that contains subdirectories for:
# - media (where your actual media files are stored)
# - plex (where Plex expects to find files)
# - cache (where cached files are moved to for performance)
UNIFIED_MOUNT_PATH=/mnt/user/library

# Application Configuration
CONFIG_DIR=/mnt/user/appdata/cacherr/config

# Cache Behavior - Optimized for hardlink support
COPY_TO_CACHE=false
DELETE_FROM_CACHE_WHEN_DONE=true
USE_SYMLINKS_FOR_CACHE=false
MOVE_WITH_SYMLINKS=false

# Media Processing Settings
NUMBER_EPISODES=5
DAYS_TO_MONITOR=99
WATCHLIST_TOGGLE=true
WATCHLIST_EPISODES=1
WATCHED_MOVE=true

# Performance Settings
MAX_CONCURRENT_MOVES_CACHE=3
MAX_CONCURRENT_MOVES_ARRAY=1

# Real-time Watching (Optional)
REAL_TIME_WATCH_ENABLED=false
REAL_TIME_WATCH_CHECK_INTERVAL=30

# Test Mode (Set to true for initial testing)
TEST_MODE=false

# Container Settings
TZ=America/New_York
PORT=5443
DEBUG=false
LOG_LEVEL=INFO

# IMPORTANT DIRECTORY STRUCTURE:
# Your UNIFIED_MOUNT_PATH should be organized like this:
#
# /mnt/user/library/
# ├── media/
# │   ├── Movies/
# │   └── TV Shows/
# ├── plex/
# │   ├── Movies/        <- Plex looks here
# │   └── TV Shows/      <- Plex looks here
# └── cache/
#     ├── Movies/        <- Files moved here for performance
#     └── TV Shows/      <- Files moved here for performance
#
# The hardlink solution will:
# 1. Move files from media/ to cache/
# 2. Create hardlinks in plex/ pointing to cache/
# 3. Plex continues to see files in plex/ location
# 4. Actual files are stored in faster cache/ location