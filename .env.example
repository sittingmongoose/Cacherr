# Copy to .env (kept out of git) for local secrets.
#
# Context7 MCP (Cursor): used by bin/setup-mcp-context7.* to generate .cursor/mcp-config.json
CONTEXT7_API_KEY=

# Optional: if you prefer pulling the key from 1Password, set one of these:
# - OP_CONTEXT7_API_KEY_REF should be a 1Password Secret Reference, e.g.:
#   op://Vault Name/Item Name/CONTEXT7_API_KEY
OP_CONTEXT7_API_KEY_REF=
